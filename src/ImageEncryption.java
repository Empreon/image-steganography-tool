import gui.EncryptionWindow;

public class ImageEncryption {
    public static void main(String[] args) {
        EncryptionWindow window = new EncryptionWindow();
        window.setVisible(true);
    }
}